config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 20

scenarios:
  - flow:
      - post:
          url: "/api/book"
          json:
            name: "Test User"
            seats: 2

